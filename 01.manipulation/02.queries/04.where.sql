-- La clausula WHERE permite restringir la consulta, para asi retornar solo los registros que se requieran
-- Los operadores de comparacion son comunmente utilizados al realizar consultas haciendo uso de la clausula WHERE
-- La siguiente consulta retorna unicamente los registros de la tabla que cumplen con la condicion especificada

SELECT * FROM table_name WHERE column_1 = value;