-- La clausula UPDATE permite editar un registro existe en la tabla
-- La siguiente clausula actualiza el valor de la columna especificada
-- La clausula WHERE permite especificar con exactitud cual es el registro a modificar
-- Sin la clausula WHERE se actualizarian todos los registros de la columna en la tabla

UPDATE table_name SET column_name = column_value WHERE id = 1;